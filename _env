# Server port configuration:
PORT=3000

# Environment (choose between: "production" or "development"):
ENVIRONMENT=development

# You can set a limit of users allowed to register. Set the value to 0 to not use the limit
LIMIT_USERS_REGISTERED=0

# Database format connection (choose between: "standard" or "DNSseedlist"):
MONGO_FORMAT_CONNECTION=DNSseedlist

# MongoDB: Standard Connection String Format
MONGO_HOST=127.0.0.1
MONGO_PORT=27017
MONGO_DB=boilerplate_database
MONGO_USER=
MONGO_PASS=

# MongoDB: DNS Seedlist Connection Format
MONGO_DNS_SEEDLIST_CONNECTION=mongodb+srv://<user>:<password>@<uri-and-options>

# JSONWEBTOKEN:
SECRET=yoursecret
DURATION=2h



aws cloudformation update-stack --stack-name bbn-backend-api --template-body file://./ecs.yml --capabilities CAPABILITY_NAMED_IAM \
--parameters ParameterKey=VPCID,ParameterValue=vpc-0a6160b57d0864fa3 \
ParameterKey=Subnet1ID,ParameterValue=subnet-074098a206f792c45 \
ParameterKey=Subnet2ID,ParameterValue=subnet-0f72a52510833db68 \
ParameterKey=DomainName,ParameterValue=bharatblockchain.io